{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "PythonService": {
    "// BaseUrl": "In-RAM single-server edition: Python runs on the QA server itself.",
    "BaseUrl": "http://localhost:8000"
  },
  "Auth": {
    "// Mode": "Windows Authentication against Active Directory in QA.",
    "Enabled": true,
    "Mode": "Windows"
  }
}
