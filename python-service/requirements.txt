flask>=3.0.0
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
python-docx>=1.1.0
cryptography>=42.0.0
pyarrow>=14.0.0
python-dotenv>=1.0.0
requests>=2.31.0
# Query-backed Data Experts (rollout batch 3). All imports are lazy — the
# service runs fine without these; only azure/sqlserver experts need them.
#   duckdb          runs the admin's SQL over downloaded lake files
#   azure-identity  service-principal (Entra app) authentication
#   azure-storage-blob  blob download
#   pyodbc          SQL Server / Azure SQL (also needs the MS ODBC Driver 18
#                   installed on the host machine)
duckdb>=1.0.0
azure-identity>=1.16.0
azure-storage-blob>=12.19.0
pyodbc>=5.0.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
# Production WSGI servers (single process for the in-RAM design):
#   waitress — Windows or Linux (used by serve.py)
#   gunicorn — Linux only (used by the Dockerfile, --workers 1)
waitress>=3.0.0
gunicorn>=21.2.0
